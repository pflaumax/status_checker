[project]
name = "status-checker"
version = "0.1.0"
description = "Telegram bot that monitors services and website on a Raspberry Pi"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.33.1",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
